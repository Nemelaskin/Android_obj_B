<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.andforobject_b">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".templateActivity"></activity>
        <activity android:name=".SensorCrudActivity" />
        <activity android:name=".PositionCrudActivity" />
        <activity android:name=".RoleCrudActivity" />
        <activity android:name=".RoomCrudActivity" />
        <activity android:name=".MainAdminActivity" />
        <activity android:name=".NewsCrudActivity" />
        <activity
            android:name=".MapViewActivity"
            android:screenOrientation="landscape" />
        <activity android:name=".NewsActivity" />
        <activity android:name=".RatingTableActivity" />
        <activity android:name=".CabinetActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".homeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AuthActivity" />
    </application>

</manifest>